@using Ascend_Forum.Core.Models.Member;

@{
    ViewData["Title"] = "Members";
}

@model IEnumerable<MemberViewModel>

<div class="text-center">
    <h1>
        @ViewData["Title"]
    </h1>
</div>

<div class="container-fluid my-5">
    <div class="row justify-content-center mb-4">
        <div class="col-md-8">
            <form method="get" class="row g-3 align-items-end">
                <div class="col-md-5">
                    <label for="minPostCount" class="form-label">Minimum Posts</label>
                    <input type="number" name="minPostCount" id="minPostCount" class="form-control" placeholder="e.g. 5" value="@Context.Request.Query["minPostCount"]" />
                </div>
                <div class="col-md-5">
                    <label for="minReactionCount" class="form-label">Minimum Reactions</label>
                    <input type="number" name="minReactionCount" id="minReactionCount" class="form-control" placeholder="e.g. 10" value="@Context.Request.Query["minReactionCount"]" />
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100">Filter</button>
                </div>
            </form>
        </div>
    </div>
    @if (Model.Any())
    {


        <div class="row">
            <div class="col">
                <div class="table-responsive">
                    <table class="table table-striped table-hover table-bordered align-middle">
                        <thead class="table-dark">
                            <tr>
                                <th scope="col">Ascend Name</th>
                                <th scope="col">Posts Count</th>
                                <th scope="col">Reactions Received</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var member in Model)
                            {
                                <tr>
                                    <td>@(member.AscendName)</td>
                                    <td>@(member.PostCount)</td>
                                    <td>@(member.CommentReactionCount)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="d-flex justify-content-center align-items-center mt-5">
            <div class="text-center p-5 " style="max-width: 500px;">
                <h4 class="fw-bold mb-2">No Members Found</h4>
                <p class="text-muted fst-italic mb-0">No members match your current filters.</p>
            </div>
        </div>
    }
</div>
